# Core dependencies
numpy>=2.3.0

# PyTorch with CUDA support (CUDA 11.8 compatible)
# Use CPU version if CUDA not available: torch>=2.7.1
torch>=2.7.1+cu118 --index-url https://download.pytorch.org/whl/cu118

